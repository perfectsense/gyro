import "project.beam" as project

AWS::Bucket "beam-enterprise-ops" {
    name: "beam-enterprise-ops"

    corsRule {
        allowedHeaders: [ "*" ]
        allowedMethods: [ "GET" ]
        allowedOrigins: [ "https://${project.SUBDOMAIN}" ]
        maxAgeSeconds: 3000 
    }
}
