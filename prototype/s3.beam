import "project.beam" as project

aws::bucket beam-enterprise-ops {
    name: beam-enterprise-ops

    cors-rule {
        allowed-headers: [ * ]
        allowed-methods: [ GET ]
        allowed-origins: [ https://${project.SUBDOMAIN} ]
        max-age-seconds: 3000
    }
}
