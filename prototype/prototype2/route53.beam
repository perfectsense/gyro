import "project.beam" as project
import "vpc.beam" as vpc

aws::hosted_zone public {
    name: "${project.subdomain}."
}

aws::hosted_zone private {
    name: "private.${project.subdomain}."
    private: true
    vpcId: ${vpc.vpc.vpcId}
}
