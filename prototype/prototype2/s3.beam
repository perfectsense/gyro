import "project.beam" as _

aws::bucket beam-enterprise-ops {
    name: "beam-enterprise-ops"

    corsRule {
        allowedHeaders: [ "*" ]
        allowedMethods: [ "GET" ]
        allowedOrigins: [ "https://${subdomain}" ]
        maxAgeSeconds: 3000 
    }
}
