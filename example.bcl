project: ops-test
serial: 99
environment: production
config vpc
    cidr: "10.0.0.0/16"
    region: us-east-1
    vpc-id: vpc-123456
    name: $(project)-$(serial)-$(environment)-$(config vpc | region)
end

config subnet
    vpc-id: $(config vpc | vpc-id)
    zone: $(config vpc | region)b
    tags:
        belongsTo: $(config vpc | name)
    end
end

