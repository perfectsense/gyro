rootProject.name = 'gyro'

include 'gyro-core', 'cli',
        'providers:aws', 'providers:openstack','providers:azure',
        'plugins:ssh', 'plugins:beam-enterprise', 'plugins:sample',
        'doclet'

enableFeaturePreview('STABLE_PUBLISHING')
