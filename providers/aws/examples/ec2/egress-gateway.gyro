import ../init as _

aws::vpc vpc-example
    cidr-block: "10.0.0.0/16"

    tags: {
        Name: "vpc-example"
    }
end

aws::egress-gateway egress-gateway-example
    vpc-id: $(aws::vpc vpc-example | vpc-id)
end